import { CreditNotesHelper } from './credit-notes-helper';

describe('CreditNotesHelper', () => {
  it('should create an instance', () => {
    expect(new CreditNotesHelper()).toBeTruthy();
  });
});
