import { SubscriptionsHelper } from './subscriptions-helper';

describe('SubscriptionsHelper', () => {
  it('should create an instance', () => {
    expect(new SubscriptionsHelper()).toBeTruthy();
  });
});
